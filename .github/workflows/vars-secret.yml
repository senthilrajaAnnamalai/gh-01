name: Explore vars and secrets
on: workflow_dispatch
env:
  AWS_Username: temp
  tf-version: 1.3.5
jobs:
  Configure:
    runs-on: ubuntu-latest
    steps:
      - name: Install terraform
        run: |
         echo $tf_version
         echo  https://releases.hashicorp.com/terraform/$tf-version/terraform_$tf-version_linux_amd64.z
  
